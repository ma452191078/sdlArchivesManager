<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdl.sdlarchivesmanager"
    android:versionCode="1"
    android:versionName="1.1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_low"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ActivityFlowChart" />
        <activity android:name=".activity.ActivityStep1" />
        <activity android:name=".activity.ActivityStep2" />
        <activity android:name=".activity.ActivityStep3" />
    </application>

</manifest>
